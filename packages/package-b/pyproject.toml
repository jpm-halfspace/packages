[project]
name = "package-b"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jens Peder Meldgaard", email = "jpm@halfspace.io" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
package-b = "package_b:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
